# SITE_ADDRESS is injected from the environment (set in docker-compose.prod.yml
# from .env). Caddy automatically obtains + renews a Let's Encrypt certificate
# for it and reverse-proxies to the app container.
{$SITE_ADDRESS} {
	reverse_proxy app:8000
}
